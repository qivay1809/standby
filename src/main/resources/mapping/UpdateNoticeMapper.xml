<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.qivay.mapper.UpdateNoticeMapper">

    <select id="UpdateNotice" resultType="String">
        SELECT config_content from config where `config_key` = #{key}
    </select>

</mapper>